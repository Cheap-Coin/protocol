{
  "name": "cheapcoin-protocol",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm --filter @cheap/protocol build",
    "lint": "pnpm --filter @cheap/protocol lint",
    "typecheck": "pnpm --filter @cheap/protocol typecheck",
    "test": "pnpm --filter @cheap/protocol test",
    "rust:fmt": "cargo fmt --all -- --check",
    "rust:clippy": "cargo clippy --locked --workspace --all-targets -- -D warnings",
    "rust:clippy:litesvm": "cargo clippy --locked --workspace --all-targets --features litesvm-tests -- -D warnings",
    "rust:test": "cargo test --locked --workspace",
    "rust:build-sbf": "node scripts/build-cheap-lock.mjs",
    "rust:test:litesvm": "cargo test --locked --features litesvm-tests --test cheap_lock_litesvm",
    "rust:artifacts:verify": "node scripts/verify-cheap-lock-artifacts.mjs",
    "rust:build:verifiable": "node scripts/build-cheap-lock.mjs --verifiable",
    "rust:audit": "cargo audit",
    "rust:deny": "cargo deny check",
    "security:deps": "pnpm audit --audit-level high",
    "deployments:validate": "pnpm --filter @cheap/protocol build && node scripts/deployments.mjs validate",
    "deployments:format": "pnpm --filter @cheap/protocol build && node scripts/deployments.mjs format",
    "deployments:env": "pnpm --filter @cheap/protocol build && node scripts/deployments.mjs env",
    "deployments:verify-chain": "pnpm --filter @cheap/protocol build && node scripts/deployments.mjs verify-chain",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm --filter @cheap/protocol test:runtime && node scripts/deployments.mjs validate"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "typescript": "7.0.2"
  }
}
