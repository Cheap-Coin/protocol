{
  "name": "@cheap/contracts",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "lint": "forge fmt --check && forge lint --deny warnings",
    "test": "forge test --force -vv",
    "prepare:analysis": "node scripts/prepare-analysis.mjs",
    "security:aderyn": "pnpm prepare:analysis && pnpm dlx @cyfrin/aderyn@0.6.8 .analysis-workspace --output aderyn-report.md --skip-update-check && node scripts/assert-aderyn.mjs"
  }
}
